This project predicts startup profits using a Decision Tree Regressor.
Multiple parameter combinations—such as depth, split criteria, and leaf settings—are tested to improve model accuracy.
The dataset is preprocessed and encoded to ensure the model captures key business features.
Each configuration is evaluated using R² score to identify the best-performing model.
The goal is to understand how decision-tree parameters impact prediction quality in real-world business data.
